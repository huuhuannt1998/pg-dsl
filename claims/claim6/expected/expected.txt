CLAIM 6
A fixed probe grid is a coverage limit, not a guarantee: at the 3 published states all 16 state-conditional mutants are invisible; at 30 random states all 16 are caught; benign false positives stay at 0 for every grid size; and narrow triggers stay about 42% invisible even at 100 states (Table 11).

EXPECTED OUTCOME
rebuttal_experiments/results/n5b_coverage_curve.json reports first_design_result with K=3 detected 0 of 16, K=30 and K=100 detected 16 of 16, benign_by_K zero at every K, and a trigger-width curve whose w=0.01 row reaches only 0.583 at K=100.

MECHANICAL CHECK
After run.sh, `python3 claims/_check.py claims/claim6` compares the fields
listed in reference.json against the result JSON and prints PASS/FAIL per
field. run.sh already does this at the end, so a plain `./run.sh` is enough.
